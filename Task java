
// Access modifiers:
  •public 
  •protected 
  •private 
إذا وضعت قبل المتغير أو الميثود بتحدد طريقة الوصول للمتغير أو الميثود 
يتم وضع ال Access modifier قبل نوع الخاصية  
Public: أقدر أستخدمه داخل الكلاس و خارج الكلاس حتي لو في باكدچ تانيه 
ال public أقدر أشوفه في باكدچ أخري 
Protected: تستخدم للكلاسات المرتبطه بالكلاس التي ترث منه و غير متاحه لأي كلاس آخر 

Private: متاحة لل methods داخل الكلاس فقط 

في حالة عدم وضع public أو private يكون ال access modifiers من النوع default 

default مقدرش أشوفه في باكدچ أخري 

ال acssess modifiers بتيجي م الميثود و بتيجي مع الخصائص






2
import java.util.Scanner;
 
 class Inheritance {
 
    public static void main(String[] args) {
        Circle c = new Circle();
        System.out.println("Enter the radious of circle");
        Scanner sc = new Scanner(System.in);
        double r = sc.nextDouble();
        c.setR(r);
        double area = c.area();
        System.out.println("The area of circle:" + area);
 
        Cylinder c1 = new Cylinder();
        System.out.println("Enter the radious of cylinder");
 
        Scanner sc1 = new Scanner(System.in);
        double r1 = sc1.nextDouble();
        c1.setR(r1);
 
        System.out.println("Enter the hight of the cylinder");
        Scanner sc2 = new Scanner(System.in);
        double r2 = sc2.nextDouble();
        c1.setH(r2);
 
        double areaOfCylinder = c1.CylinderArea();
        System.out.println("The area of Cylinder:" + areaOfCylinder);
        double VolumeOfCylinder = c1.Volume();
        System.out.println("The volume of cylinder:" + VolumeOfCylinder);
 
    }
}
 
public class Circle {
 
    private double r;
    final double pi = 3.14;
 
    public double getR() {
        return r;
    }
 
    public void setR(double r) {
        this.r = r;
    }
 
    public double area() {
        double r = getR();
        double area = pi * Math.pow(r, 2);
 
        return area();
 
    }
}
 
public class Cylinder extends Circle {
 
    private double h;
 
    public double getH() {
        return h;
    }
 
    public void setH(double h) {
        this.h = h;
    }
 
    public double CylinderArea() {
        double r = getR();
        double h = getH();
 
        double area = (2 * area()) + (pi * 2 * r * h);
 
        return area;
 
    }
 
    public double Volume() {
        double r = getR();
        double h = getH();
        double Volume = h * area();
        return Volume;
 
    }
}





3
import java.util.Scanner;
 
 class Array1 {
 
    public static void main(String[] args) {
        int a[] = {55, 91, 34, 87, 10};
        System.out.println("Enter the Index");
 
        try {
            Scanner sc = new Scanner(System.in);
            int theIndexFromIUser = sc.nextInt();
            System.out.println(a[theIndexFromIUser]);
 
        } catch (IndexOutOfBoundsException e) {
            System.out.println("Enter the index from 0 to 11");
        }
    }
}
